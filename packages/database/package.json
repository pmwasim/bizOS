{
  "name": "@bizo/database",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "prisma generate && tsc -p tsconfig.build.json --emitDeclarationOnly && node scripts/build-runtime.mjs",
    "clean": "rm -rf generated dist coverage tsconfig.tsbuildinfo",
    "lint": "eslint prisma.config.ts src",
    "prisma:format": "prisma format",
    "prisma:generate": "prisma generate",
    "prisma:migrate:deploy": "prisma migrate deploy",
    "prisma:migrate:dev": "prisma migrate dev",
    "prisma:seed": "node scripts/build-seed.mjs && prisma db seed",
    "prisma:validate": "prisma validate",
    "test": "vitest run --passWithNoTests",
    "test:coverage": "vitest run --coverage --passWithNoTests",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@bizo/contracts": "workspace:*",
    "@prisma/adapter-pg": "7.9.1",
    "@prisma/client": "7.9.1",
    "pg": "8.22.0"
  },
  "devDependencies": {
    "@types/node": "26.2.0",
    "@types/pg": "8.20.3",
    "dotenv": "17.4.2",
    "esbuild": "0.28.2",
    "prisma": "7.9.1",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
