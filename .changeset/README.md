# Changesets

Changesets will drive release notes and semantic versions once the first releasable capability is
approved. Phase 0 packages are private and versioned `0.0.0`; foundation-only pull requests do not
need a changeset.
